import React from 'react';
import { Typography } from '@material-ui/core';

const Brand = props => {
  return <Typography {...props}>News System - Administration Area</Typography>;
};

export default Brand;
